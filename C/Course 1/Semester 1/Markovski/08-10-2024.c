#include <stdio.h>

int main()
{
    // int a;
    // int b;

    // a = 2;

    // b = a % 2;
    
    // int isEven = 1;

    // printf("%d", isEven);

    // int a = 29;
    // int b = 5;
    // int c;
    // int d;

    // c = a / b;
    // d = a % b;

    // printf("Division quotient: %d\n", c);
    // printf("Division remainder: %d\n", d);

    // int a = 5139;

    // int b = a / 1000;
    // int c = (a / 100) % 10;
    // int d = (a / 10) % 10;
    // int e = a % 10;

    // printf("The Digits of the Number are:\n");
    // printf("%d\n", b);
    // printf("%d\n", c);
    // printf("%d\n", d);
    // printf("%d\n", e);

    // int f = (a / 10) % 100;

    // printf("The Number in the middle is: %d\n", f);
    
    // int g = b + e;

    // printf("The Sum of the first and last digit is: %d", g);

    // int a = 265;
    // int b = 44;
    
    // (a < b) && (a = b);

    // printf("%d", a);

    // int a = 25;
    // int b;

    // b = (a <= 6)&&(a >= 2);

    // printf("%d\n", b);

    // int a = 265;
    
    // int b = a / 100;
    // int c = (a / 10) % 10;
    // int d = a % 10;

    // int e = b + c * 10 + d * 100 + 1;

    // printf("%d\n", e);

    // int cm = 3;
    // double inch = 3 * 0.393701;

    // printf("%f\n", inch);

    // int a = 5;
    // int b = 7;

    // int c = (a + b)*(a + b)*(a + b)*(a + b) - (a - b) * (a - b);

    // printf("%d\n", c);

    int money = 193;

    int hundreds = money / 100;
    int ten = (money / 10) % 10;
}